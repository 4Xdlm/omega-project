{
  "name": "omega-memory-hook",
  "version": "3.20.1",
  "description": "OMEGA Phase 20.1 — Gateway Memory Hook Runtime",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["omega", "memory", "hook", "runtime", "lifecycle"],
  "author": "OMEGA Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
