{
  "name": "omega-integration-layer",
  "version": "3.20.0",
  "description": "OMEGA Phase 20 — Integration Layer (Memory + Persistence)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["omega", "integration", "memory", "persistence", "nasa-grade"],
  "author": "OMEGA Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
