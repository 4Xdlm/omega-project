{
  "name": "@omega/memory-layer",
  "version": "1.0.0-NASA",
  "description": "OMEGA Memory Layer — NASA-Grade L4 / DO-178C Level A",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
