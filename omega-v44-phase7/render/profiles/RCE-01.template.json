{
  "profile_id": "RCE-01-PREMIUM",
  "profile_version": "1.2",
  "viewport": {
    "width": 512,
    "height": 512
  },
  "calibration": {
    "anisotropy_min": "${ANISO_MIN}",
    "anisotropy_max": "${ANISO_MAX}",
    "opacity_base": "${OPACITY_BASE}",
    "opacity_z_coefficient": "${OPACITY_Z_COEF}",
    "oxygen_amplitude_max": "${OXYGEN_AMP_MAX}",
    "render_timeout_ms": "${RENDER_TIMEOUT}"
  },
  "rendering": {
    "device_scale_factor": 1,
    "color_space": "sRGB",
    "path_resolution": 360,
    "base_radius": 200
  }
}
