{
  "name": "@omega/v44",
  "version": "1.0.0",
  "description": "OMEGA V4.4 - Phases 1-6 Implementation (NASA-Grade L4)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "test:contract": "vitest run test/contract",
    "test:determinism": "vitest run test/determinism",
    "test:perf": "vitest run test/performance",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src test --ext .ts",
    "type-check": "tsc --noEmit",
    "cli": "node --loader ts-node/esm src/phase6_cli/bin.ts"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "omega",
    "emotional-analysis",
    "nasa-grade",
    "deterministic"
  ]
}
