{
  "name": "@omega/plugin-gateway",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "OMEGA Plugin Gateway — single entry/exit point for external modules. INV-PNP-01.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fast-json-stable-stringify": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.0.17"
  }
}
