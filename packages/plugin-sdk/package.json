{
  "name": "@omega/plugin-sdk",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "OMEGA Plugin SDK — kit for building compliant plugins. Schema-first, compliance-gated.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fast-json-stable-stringify": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.0.17"
  }
}
