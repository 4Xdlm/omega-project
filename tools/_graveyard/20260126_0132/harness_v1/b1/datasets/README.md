# B1 Datasets

Place test datasets here for B1 Stability at Scale tests.

## Requirements

- Deterministic content (no random generation)
- Offline only (no network fetches)
- Hash-verifiable

## Format

TBD during B-EXEC phase.
